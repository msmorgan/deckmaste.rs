# engine-act-facet-contract — SDD progress ledger

Plan: docs/superpowers/plans/2026-07-16-engine-act-facet-contract.md
Spec: docs/superpowers/specs/2026-07-16-engine-act-facet-contract-design.md
Workspace baseline (@ before Task 1): 38d3f75c105f (empty, parent aeb56010 "refactor: table-driven parser dispatch replaces if-let ladders")

Format: Task N: complete (commits BASE..HEAD) — suite summary

## Minor findings roll-up (for final whole-branch review)

- Task 1: fizzle test placed in `resolve/action.rs` internal `#[cfg(test)]` mod instead of `crates/deckmaste_engine/tests/` (brief said tests/, defensible per harness-style match to sibling tests).
- Task 1: implementer report's self-review section cites `[CR#616.1]` as verified for this task's diff but that citation does not appear anywhere in the diff (report narrative inaccuracy only, not a code defect).

## Task log

Task 1: complete (commits aeb56010cf02..5219ca0ae4bd) — core: 219 passed/0 failed; engine: 771 passed/1 ignored/0 failed; cards: 286 passed/0 failed (wipe-first wizards regen unchanged shape). Decision: kept Move as tuple variant with `from` as 4th trailing-default slot (struct variant would break positional `Move(This, Graveyard)` reads); extended macro_ron/macro_ron_derive with new `PairPlusTwoDefaults` (reviewed & approved as justified necessary infra, not scope creep). Reviewer: sonnet, Approved, 0 Critical/Important, 2 Minor (above).

Task 2: complete (commits 5219ca0ae4bd..5bce7718719a) — core: 219 passed/0 failed; engine: 774 passed/1 ignored/0 failed (+3 new); cards: 286 passed/0 failed (wipe-first regen; one no_dead_grammar_nodes gate hit + fixed via reviewed allowlist entry for shell `Batch`). InHand.ron placed under macros/filter/ (no predicate/ dir exists) with body `And([InZone(Hand), Owner(Ref(Param(0)))])` (brief's draft syntax doesn't exist in this grammar). Reviewer: sonnet, Approved, 0 Critical/Important, 2 Minor (SHELL: allowlist prefix vocab inconsistency; no dedicated idris_emit unit test for Batch, consistent with Repeat's precedent).

Task 3: complete (commits 5bce7718719a..7c2779622c51) — engine: 545 passed/0 failed/1 ignored (lib) + all 15 integration/doc-test binaries green. Fix: `affected_player` branches on `x.zone` (Battlefield|Stack -> controller; else -> owner via `state.cards.get(card).owner`, or controller for player-proxy objects) — neither of the brief's two speculative branches was literally correct (controller==owner off-battlefield is a behavioral guarantee of one call site `apply_zone_will_change`, not a type invariant; Stack is also controller-bearing, already flagged in brief's own draft). Self-caught mis-citation (an unrelated target-legality rule) replaced with [CR#109.4]/[CR#108.4,108.4a] before commit. Reviewer: sonnet, Approved, 0 Critical/Important, 2 Minor (report overstated novelty of Stack-inclusion finding; .sdd-progress.md orchestrator bookkeeping rode into this commit, self-disclosed, harmless).

Task 4: complete (commits 7c2779622c51..97fea50cafb4) — engine: 545 passed/0 failed/1 ignored (lib, exact baseline parity) + all 15 integration binaries; cards: 286 passed/0 failed; migrations: 397 passed/0 failed (no wizards regen needed, engine-internal type). Representation-only merge of ZoneWillChange/ZoneChanged into one GameEvent::ZoneChange{snapshot:Option<Box<LkiSnapshot>>,...} (None=future/replaceable, Some=past/recorded) — 205-site sweep, confirmed zero test-assertion-meaning changes (only mechanical rename+guard). Also fixed 2 sites in crates/deckmaste_noncanon/src/probe.rs (outside brief's file list, needed for workspace to compile; reviewer confirmed narrowly-scoped and correct). Reviewer: sonnet, Approved, 0 Critical/Important, 1 Minor (3 stale doc-comment references to old variant names in deckmaste_core/deckmaste_migrations, cosmetic).

Task 5: complete (commits 97fea50cafb4..ab75dd3dbc89, disclosure follow-up cf455c479105) — engine: 550 passed/0 failed/2 ignored (lib) + all integration binaries; cards: all green (no wizards regen needed, engine-internal GameEvent change, no serde/core touch). "The heart": Act gains committed:bool phase marker (mirrors Task 4's ZoneChange.snapshot law) + contents:Option<Box<ActContents>> for verbs needing body unwrap at apply (mill's top-slice group, scry/fight body). One [CR#616.1] window per entity Act; resolve fizzles on unresolvable coordinates (vec![], no trace); apply of a passed future Act commits contents directly (no second window); FinalizeAct{watch,mark} observes resolution_events[mark..] and records the committed:true fact iff the verb's characteristic change landed (redirect-safe/Megrim-under-madness, suppression-safe/regeneration+all-canted-mill). Deleted composite_body_head_move_to/composite_body_group_move/composite_body_acts. All 6 ticket-item tests green. Each's batcher fixed to preserve simultaneity now that act_window returns [Emit,FinalizeAct] not a single Emit. Reviewer: opus, first pass Needs fixes (1 Critical: pre-existing test rest_in_peace_replaces_milled_cards_with_exile was disabled by this task and the report/commit message falsely claimed no expectation changed — disclosure-only finding, no engine-correctness bug). Fixed via follow-up commit cf455c479105 (comment/ignore-reason-only, reviewed by sonnet, Approved) that names two Task-7 obligations at the test site. **Task 7 obligation (verbatim, must be in Task 7's brief and its review gate):** "Re-enable rest_in_peace_replaces_milled_cards_with_exile (remove its #[ignore]) and make it pass — the per-card entity Act bodies must give a result-replacement a bindable patient and an honored destination. Both gaps are documented at the test site (crates/deckmaste_engine/src/resolve/action.rs, near the #[ignore] on that test)."

Task 6: complete (commits fb9433a7f13b..9e9ce7e7a60b, fizzle-discipline follow-up 940d868a3530) — engine: 554 passed/0 failed/2 ignored (lib) + all integration binaries. `Batch(n, Act(verb))` now resolves to ONE future aggregate `Act{batch: Some(n)}` (not n separate futures) so count-doubling replacements (Bruvac shape) bite before any card moves; [CR#614.5] tree-scoping (`inherited`/`inherited_seed`, threaded from `replace_event`) stops a replacement re-catching its own products (Archive shape); containment suppression (`contained: true` on each of the n contained per-entity futures a passed aggregate's apply schedules) keeps result-level triggers per-card but ACT-level triggers fire once per batch via `FinalizeWatch::AnyContained` (Task 5's reserved 4th variant); outer-before-inner [CR#616.1g] ordering falls out structurally (contained futures don't exist as WorkItems until the aggregate's own apply runs). All 3 required scenarios (Bruvac/Archive/aggregate-trigger-once) green, verified via revert-probes (temporarily broke each of the 3 non-obvious mechanisms, confirmed predicted failure, restored). Reviewer: sonnet, first pass Needs fixes (1 Important: new `batch_act_head` helper's fizzle discipline was weaker than `composite_items`'s for Mill/empty-library, Scry-family/`composite_body_would_act` not called, Destroy+Fight/gone-patient not checked — real narrow gap, not the "parallel path" the plan's ADAPT-POINT warned about, which was correctly avoided elsewhere). Fixed via follow-up commit 940d868a3530 (wired the same helpers `composite_items` uses into `batch_act_head`, verbatim-matching logic confirmed by reviewer; new test `batch_mill_over_empty_library_fizzles_the_whole_aggregate`), re-reviewed by sonnet, Approved.

## Escalation + ruling: Task 7 Mill shape (before Task 7 started)

Task 7's implementer was dispatched, read the full task + spec, and self-blocked before writing any code: the brief's Mill formula (`With(Existing(TopOfLibrary(N)), Each(..., Act(Composite("Mill", ...))))`, a per-card fan-out mirroring Discard) would regress two landed Task 5/6 tests (`mill_clamps_to_library_size_as_one_batch`, `act_mill_fires_post_commit_and_not_from_an_empty_library`, both asserting ONE simultaneous batch / ONE aggregate fact) and conflicted with the spec's own `Past(Batch)` finalization language and Task 6's own Bruvac-shape framing (`would: <batch of Act(Mill)>` needs something batch-shaped to match). Escalated per the "genuine design fork the plan doesn't settle" rule rather than improvising. Orchestrator relayed the fork upward; ruling received and applied:

**RULING (2026-07-16): Mill = `Batch(Param(0), Act(Composite(name: "Mill", body: <late-bound top-slot move, from: Library, to: Graveyard>)))`** — mill is slice-family like Draw/Scry, rides Task 6's Batch containment verbatim, NOT a `With/Each` per-card fan-out. Keeps both landed tests green as-is (no behavioral assertion changes). `rest_in_peace_replaces_milled_cards_with_exile` must be re-enabled and made to pass IN Task 7 (Batch containment's per-contained-future bindable patient + honored destination closes both documented gaps). Choice verbs (Discard) keep `With/Choose/Each`; slice verbs (Mill/Draw/Scry/Surveil/Fateseal) ride `Batch`. Plan (Task 7 Step 2) and spec (expansion table + per-verb table) both corrected in place to reflect this — re-read before use, do not trust a cached/earlier read of either file for Mill's shape. Also blessed as ordinary implementation judgment (not forks): `Action::Composite` becomes a struct variant `{ name: VerbName, body }` (matches `KeywordAbility::Composite` precedent); Discard keeps its bespoke `DiscardCards`/`DiscardRandom` choice path this task, re-keying only the atom (the `With(Choose(...))` reshape is Task 8's, per the plan's own "Task 8 reshapes discard's choice" line).

Task 7: complete (commit f52e919d1aca) — core: 219 passed/0 failed; cards: 185 lib + all integration binaries, 0 failed/0 ignored (wipe-first regen, byte-identical card text proof green); engine: 555 passed/1 ignored (unrelated reconfigure seam)/0 failed; migrations: 397 passed/0 failed. Implements the Task 7 Mill-shape ruling exactly: `KeywordAction` enum deleted (zero remaining Rust references); `Action::Composite` is now a struct variant `{name: VerbName, body}`; Mill/Draw both `Batch(Param(0), Act(Composite(name, body: <top-slot move>)))`, riding Task 6's containment verbatim — the old Task-5 interim aggregate-only Mill apply replaced (not left as dead code) with a per-card `Occurrence::Batch` of individually-replaceable futures. `mill_clamps_to_library_size_as_one_batch`/`act_mill_fires_post_commit_and_not_from_an_empty_library` unchanged (construction syntax only). `rest_in_peace_replaces_milled_cards_with_exile` un-ignored and genuinely passing (substantive per-card redirect assertion, not weakened) — Task 7 obligation from Task 5's deferral closed. New (sound, brief-blessed) mechanism: `BatchActHead.from = Some(Library)` for Mill only, giving a source-scoped `CantHappen(from: Library)` power to suppress the WHOLE aggregate while a destination-scoped Instead (Rest in Peace) bites only the per-card contained futures, leaving the aggregate's own `Act(Mill)` fact alone [CR#701.17c] — tested in both directions. Discard's bespoke choice path left untouched this task (atom re-keyed only, `With(Choose)` reshape deferred to Task 8 per ruling). Cost/dispatch sites re-keyed by verb name throughout. `idris/src/Core.idr` intentionally untouched (Task 11's job); `idris_emit.rs` Rust-side logic correct, its unit tests green. Reviewer: opus, Approved, 0 Critical/Important, 4 Minor (an `.expect()` vs graceful-fizzle inconsistency in Mill's apply arm — structurally unreachable but stylistically inconsistent with Draw's arm; cross-crate peel-helper duplication; byte-identical claim rests on the (green) cards suite the reviewer couldn't re-run; `plugin.verbs` side-table populated-but-unread this task, deliberate Task-9 scaffolding).

Task 8: complete (commit ca6364293e90) — core: 219 passed/0 failed; engine: 556 passed/0 failed/1 ignored (unrelated); cards: all lib+integration green (wipe-first regen); migrations: 397 passed/0 failed; full `cargo test --workspace` (incl. deckmaste_tui) green. Discard rebuilt as ordinary `With(Choose/Random(InHand(who)), Each(Act(discard_what(It))))` via a carry-future `Act` (whole-instruction cant-gate before any card picked); `WorkItem::DiscardCards`/`DiscardRandom`/`decide.rs::discard_batch` deleted; `Selection::FromHand` deleted everywhere (zero references, Rust and Core.idr). `PendingDecision::DiscardCards` (distinct from the deleted WorkItem) deliberately kept — still serves DiscardToHandSize [CR#514.1], confirmed genuinely a different, still-reachable mechanism. TDD caught and fixed a real double-fire bug (Megrim-shape trigger firing twice per discard) via a new `verb == "Discard"` branch in step.rs's shared reorder/fight carry-unwrap bucket that plants no outer FinalizeAct for Discard specifically (whenever-you-discard is per-card-visible, unlike Mill/Scry's one-fact-per-instruction grain) — reviewer traced the other 5 verbs' dispatch and confirmed they're architecturally untouched by this branch. Also landed two general (non-discard-specific) fixes: binder_choice threads the frame watcher through Choose/ChooseOne (was a todo!() panic path for any frame-relative predicate, now fixed for all consumers, strict superset/no behavior change for frame-independent filters), and new sample_random_binder makes Selection::Random a live general feature (was dormant/manual-only). Core.idr FromHand deletion succeeded cleanly (3 sites) plus Macros.idr's 4 discard sugar functions rewired (idris2 --build exits 0) — reviewer judged this the unavoidable mechanical consequence of the type deletion, not scope creep. Reviewer: sonnet, Approved, 0 Critical/Important, 2 Minor (idris_emit non-You-subject discard transcription gap, disclosed/deferred, inert on current corpus; report's Concerns section slightly overstated PendingDecision::DiscardCards's liveness vs. the more precise in-code comment).

## Escalation + ruling: Task 9 pattern-twin surface (before Task 9 started)

Task 9's implementer read the full task + spec and self-blocked before writing any code: the plan stated the pattern surface two incompatible ways — `EventFilter::Act` as a clean struct variant `{verb, who, on, cause}` (Interfaces/spec) vs. existing nested spellings (`Act(Destroy(Ref(This)))`, `Act(Scry(You))`) "keep parsing" via a loader-derived twin nested INSIDE `Act(...)` (brief text/Step-1 tests/spec). Proved from macro-engine source (`macro_ron_derive::generate`, `macro_ron::expand`) that a struct variant's content reads under `Intercept::SkipStructs`, and struct positions are not macro-interceptable there (needs `Intercept::Full`) — so a macro named e.g. `Destroy` cannot expand nested inside a struct-variant `Act(...)`; only a BARE-VERB TOP-LEVEL `EventFilter` macro can (the existing `Dies`/`Destroyed` precedent). Also flagged a second gap: Task 7's side-table (`VerbDecl{name, params}`) carries only param types, not the who/on slot mapping a twin needs (Discard's action is one `Count` but its pattern needs two predicate slots) — "loader-derives-the-twin" wasn't specified precisely enough to implement. Escalated per the same "plan states a load-bearing shape two incompatible ways" rule as Task 7's Mill fork, since resolving it either changes authored RON surface text (2 corpus files) or the core type shape — not something to improvise per this project's own no-undiscussed-surface-restructuring ruling. Orchestrator relayed upward; ruling received and applied:

**RULING (2026-07-16): R1(a) — bare-verb, TOP-LEVEL, hand-authored pattern twins.** `EventFilter::Act { verb, who, on, cause }` stays the clean struct master form exactly as specified. Pattern twins are 8 hand-authored `.ron` files under `plugins/builtin/macros/filter/` (NOT loader-derived — YAGNI for a fixed set of 8 verbs, no new role-tag surface added to the side-table): `Destroy(pred)`, `Discard(who, what)`, `Mill(who)`, `Draw(who)`, `Scry(who)`, `Surveil(who)`, `Fateseal(who)`, `Fight(a, b)` — each expanding to the Act master form, mirroring the existing `Dies`/`Destroyed` precedent exactly. The nested `Act(Verb(...))` spelling retires; migrate `Regenerate.ron` + the Indestructible macro's `Act(Destroy(..))` spellings to bare `Destroy(..)`, plus whatever Rust construction sites the compiler flags. Rationale for the ledger: the nested spelling was the spec's own drafting artifact, not a settled surface (the user's original madness sketch already wrote bare `Would(Discard(This))`); R2 (preserving the literal nesting by making `Act` wrap a macro-hosting enum) was strictly worse per the same source-level proof. Plan (Task 9 Files/Test blocks) and spec (Patterns section + Madness example) both corrected in place — re-read before use. **Note for Task 9's own Step-1 test-comment draft:** it still says "Act(Destroy(Ref(This))), Act(Scry(You))... all still parse" — that line is stale prose describing the OLD nested spelling; the authoritative Files/Test bullets (and this ruling) say bare-verb forms are what must parse after migration. Don't be misled by the stale comment.

Task 10: complete (commits 1eeb24e663ef..d661d3b64a1e, citation follow-up 45d8ce6e7556) — engine: 563 lib + all integration binaries green; core 220, macro_ron 107, macro_ron_derive pass; cards: 186 + all binaries green (wipe-first regen). Madness works end-to-end. Key pieces: `Cast(Reference, Option<Cost>)` gained an alternative-cost facet as a trailing-default tuple (same Task-1 tuple/struct process; new `macro_ron::SinglePlusDefault<A,B>` visitor, mirrors PairPlusTwoDefaults precedent), threaded through the whole cast-from-resolution path with [CR#118.9]-correct affordability gating. `Produce` binder confirmed ALREADY wired pre-task (verified, not built). `Delayed`/`Reflexive` unified via `scan_created_reflexive`, with a load-bearing fix: reflexive EventObject now chases the remint through moved_chain (was resolving to a stale pre-remint id and fizzling). `Madness.ron` authored — but with ONE explicit, self-disclosed deviation from the spec's literal RON: the `instead:` body is NOT re-wrapped in `Composite(name:"Discard",...)`. Opus reviewer scrutinized this as the review's central question and independently verified — against Task 5's actual landed FinalizeAct code, not the report's prose — that this is SOUND: FinalizeAct's watch is purely observational (mark-based, scans resolution_events for ANY committed move of the same patient, mechanism-agnostic), so it survives a name-match replacement structurally; the literal re-wrap would have fizzled against composite_items's Discard arm (which doesn't recognize With(Produce(...)) bodies) and would have required new architecture + risked double-committing the fact. Judged NOT escalation-worthy (unlike Tasks 1/7/9's genuine impossibilities) since the observable behavior is provably equivalent to the spec's CR#702.35a-cited intent and the [CR#616.1f] stacked-madness behavior actually matches the CR's own worked example better than the spec's content-guard framing did. All 5 required madness-matrix scenarios pass (Megrim fires under redirect; both Leyline/madness orders owner-chosen, either exiles; stacked madness applies exactly once; declined cast's graveyard move bitten by Rest-in-Peace; alt-cost cast pays {1}{R} not printed {3}{R}, exile empties). Also: off-battlefield self-replacement gathering added ([CR#702.35a] — a static replacement whose source is the affected, off-battlefield object now functions; scoped to exclude ZoneChange-to-Battlefield to avoid double-applying AS-ENTERS self-replacements against a pre-mint id, fixed 4 regressions this surfaced). Shield rework: `CreateReplacement.subject` (authored field) retired; `create_shield` reads the protected permanent from the enclosing `With`'s `That` binding; `Regenerate.ron` re-spelled through `With`; `floating_watches` unchanged (keys on frozen subject identity already). Reviewer: opus, first pass Needs fixes (1 Important: fictitious `[CR#614.5c]` — CR 614.5 has no lettered subrules and is the wrong topic entirely — introduced on the new off-battlefield self-replacement gather, would likely trip Task 11's `cite bless`). Fixed via follow-up commit 45d8ce6e7556 (dropped 614.5c, landed on `[CR#702.35a]` alone after verifying against real CR text and correctly declining `[CR#614.15]` since that citation is already correctly reserved elsewhere in the same file for a genuinely different, still-deferred seam), re-reviewed by sonnet, Approved. Minor findings (not fixed, informational): brief's cycling-cost-discard scenario not literally exercised (matrix test 5 uses direct discard_what, not a cost-window); `[CR#701.9c]` (pre-existing pattern from Task 5) is imprecise for "redirected discard still discards" since madness exiles to a public zone, not hidden — flag for Task 11's citation audit; a debug_assert-guarded unwired case (choosable-symbol alt cost + non-mana toll) is a labeled seam, inert since madness is mana-only.

Task 9: complete (commit 7e87d9a4b6d9) — core: 220 passed/0 failed; engine: lib 557 passed/0 failed/1 ignored + all integration bins green; cards: lib 186 + all integration bins green (wipe-first regen, 7783 files, every pre-existing Act(..)-shaped spelling reads/behaves through its new bare-verb form). Implements the R1(a) ruling exactly: `KeywordActionPattern` deleted (0 refs); `EventFilter::Act{verb,who,on,cause}` is the clean struct master form; 8 hand-authored bare-verb top-level twins mirroring Dies/Destroyed precedent structurally. Lane-split matching is the architectural payoff: name-fact match (verb+coords+cause) applies in ALL lanes; a DIVERGENCE-ONLY content guard (sourced from the entailment table, same from/to facets the pre-existing ZoneChange relaxation already reads) applies ONLY in Lane::Replacement — fails only when the verb fixes a coordinate AND the event exposes it AND it's diverged from canonical; a None on either side (aggregate/pre-choice window, rowless verb) falls back to name-match. This gives the stacked-madness auto-guard (Falkenrath Gorger's choose-one, Task 10/11) with ZERO authored clause. New 3-property test proves it directly: pristine discard matches would; redirected (→Exile) discard auto-guards OUT of Replacement lane; same redirected discard STILL matches in Trigger lane. Pre-existing ZoneChange-vs-Act relaxation kept (serves a different, non-subsumed arm); replacement_watches needed no change. Regenerate.ron/Indestructible.ron migrated to bare form. Reviewer: opus, Approved, 0 Critical/Important, 3 Minor (Draw has no entailment row so its would-guard is vacuous by omission not by nature — real small asymmetry, disclosed, zero current impact since no corpus card content-guards a draw; no_dead_grammar.rs's rowless-verb allowlist is a hardcoded 4-verb set requiring future maintenance; twin render templates authored-but-inert, mirrors Dies/Destroyed precedent, no current fidelity regression).

## Escalation + ruling: Task 11 Falkenrath Gorger conferral gap (before canon cards authored)

Task 11's implementer verified (code trace + a temporary runtime probe, since removed) that Gorger's conferral ("each Vampire creature card you own that isn't on the battlefield has madness") is fully EXPRESSIBLE at the authoring surface and CORRECTLY CONFERRED by the layer machinery even off-battlefield (confirmed: the conferred Madness ability, with `ManaCostOf(This)` intact, lands in the derived ability list of an in-hand vampire) — but is never GATHERED: `replace_registry::gather_applicable`'s three sources (battlefield sweep, off-battlefield self-replacement sweep, floating shields) all read `derive::abilities_of_source`, which is PRINTED-only by its own doc comment ("Granted/conferred abilities are a later stage"); the DERIVED accessor that folds in conferrals is consulted nowhere in the replacement gather or the trigger scan. Runtime-proved: a vanilla in-hand vampire + Gorger on the battlefield, discarded → no replacement window opens, card goes straight to graveyard. This is broader than madness — no conditionally-conferred replacement or triggered ability works off the battlefield today. Per the brief's own pre-flagged instruction ("if the conferral surface can't express it yet, STOP and surface the gap ... do not invent a bespoke one-off mechanism to fake the conferral"), the implementer stopped cleanly: no canon cards authored, no idris touch, no gate run, no commit. Escalated with three options (wire conferred-ability gathering as its own engine change then re-dispatch Gorger; descope Gorger and ship Anje's Ravager alone; narrow wiring of just the off-battlefield sweep).

**RULING: option 1 — wire conferred-ability gathering as its own focused engine fix, reviewed independently, THEN dispatch Task 11's canon/idris/gate work with BOTH canon cards.** (User confirmed directly, "1", mid-escalation — a subsequent "coordinator"-labeled message briefly overrode this with option 2 and had already minted a follow-up ticket + rescoped the spec to Gorger-only; that override was itself retracted moments later once the "1" was verified as the user's own direct answer, restoring option 1 as the actual governing ruling. The interim ticket file `docs/tickets/planned/engine-conferred-ability-gathering.md` was deleted — the work is in-feature, not deferred.)

Landed as **Task 10b — Conferred-ability gathering**, inserted into the plan between Task 10 and Task 11 (Task 11's Gorger step now explicitly depends on it). Design guidance (settled, from the plan): both the replacement gather (`replace_registry.rs` battlefield sweep + off-battlefield self-replacement path) and the trigger scan's ability enumeration substitute the DERIVED ability view for the printed-only `derive::abilities_of_source` accessor, evaluated against CURRENT committed state (a replacement's applicability is judged when the event would occur, before it applies — no recursion into the pending event). Candidate sets unchanged (battlefield sweep + affected-object self-path + shields); only the ability source per candidate changes. `layer.rs:796`'s battlefield-only working-set note gets reconciled to observed behavior, not special-cased. Perf: acceptable per the project's perf-last priority, hot-path cost noted in a comment, no caching this feature. Spec's canonize section restored to the full pair (Gorger + Anje's Ravager) with a new paragraph carrying this same guidance.

## Operational note (workspace hygiene from Task 5 onward)

A concurrent session was observed touching this same jj workspace during Task 5's fix pass (an uncommitted `.sdd-progress.md` edit appeared/raced independent of dispatched subagents). From here on, every worker brief instructs: run `scripts/jj st` before committing, and if files outside the task's own scope show as modified, commit ONLY the task's own paths (`scripts/jj commit <files...> -m "..."`) rather than a bare `scripts/jj commit -m "..."` that would sweep in unrelated changes. All child dispatches are synchronous (`run_in_background: false`) from here on — a backgrounded subagent dies with no notification if its parent orchestrator turn ends before it finishes.

