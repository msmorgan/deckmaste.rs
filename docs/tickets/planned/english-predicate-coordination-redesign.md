---
needs: [english-predicate-coordination-residue]
design: true
---
**Remaining shared-subject predicate coordination that still needs redesigned
host or quantification machinery.**

- Finite asyndetic runs such as `gets ..., has ..., and is ...` need a
  categorical host discriminator. The previously attempted continuation-only
  allowance admitted render-identical trigger misparses; it must keep
  `coordinated_donated_agreement_does_not_adopt_a_bare_imperative_tail`,
  `coordinated_trigger_event_is_unchanged`, and
  `paragraph_initial_trigger_with_a_coordinated_event_still_recovers` green.
- `and is every/all creature type` needs typed universal type-set
  quantification before coordination. Do not admit the current opaque `every`
  noun, add a spelling check, or widen to a generic copular remainder.
- Clause-level attached continuations (`as though`, trailing `unless`/`if`)
  and independently failing hosts remain composition work; lexical `but`/`yet`
  gaps stay with `english-opacity-residue-families`.

Standard constraints apply.
