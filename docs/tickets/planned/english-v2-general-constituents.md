---
needs: [english-v2-frame-key-reduction]
---
Adopt v1's constituent inventory on v2's mechanism (taxonomy audit
2026-09-02, docs/memory/scratch/plan09-postmortem/taxonomy-v1-v2.md —
design only; v1 is deletion-slated and no code, type, or feature
vocabulary is ported).

RULING PREREQUISITE (lands first, user-ruled): a single derived
attachment rule in the rewrite ADR — verb-selected PPs attach at the
frame (already ruled); adjunct PPs/adverbials attach by one stated
structural rule, never by per-construction guards. Generalizing
attachment will surface genuine ties the literal-beats-nonterminal bias
currently hides; each is a STOP until the rule is written.

Adopt (each general, one construction, category-general where v1 was):
A1 one PP construction with a preposition lexical slot (v2 has 13
per-preposition categories and no PP); A2 repeatable, order-free NP
postmodifiers (v2 fixes one slot each: relative -> locative -> with);
A3 a predicate-adjunct class (temporal/locative/manner/purpose/duration
as VALUES of one class; G2's temporal prefix lands here); A4 one
subordinate-clause construction with a subordinator slot (v2: 25
constructions, per subordinator x finiteness); A5 relative-marker slot
{that, who, zero} with one general relative body ("who" has 143 corpus
witnesses); A6 recipient/retained-object passive as frame data; A7
predicative complement = AdjP | NP | PP, one construction (v2: 8 of 9
are game-meaning-partitioned); A8 polarity and card-type as features,
not constructions (24 of 34 modifiers are per-card-type); A9 modal
ellipsis; A10 arithmetic values ("half … rounded up"); A11 gerund
clauses. Probe set: the audit's 33-item table (§6) — the 5 ordinary
sentences v2 fails and v1 parses must pass.

Keep from v2 (v1 was right to lose): declared valences with no OPEN
default, frame-level verb-selected PPs, no recovery nodes, derived
separators, declared document roots, self-reference as identity provider.

Acceptance: coupled replacement (each per-X family deleted with its
general construction landing), zero net coverage loss, ratchet up,
zero ties or STOP, Rosewater share at phrase/clause level measurably
down. Standard constraints apply.
