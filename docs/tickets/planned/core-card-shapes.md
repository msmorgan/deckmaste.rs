---
needs: []
---
`Card` variants beyond `Normal`/`ModalDfc`: transform, saga, adventure, split,
flip, etc. (see the census §4 layout table). Grammar prerequisite for most
layout-specific engine and extraction work.
