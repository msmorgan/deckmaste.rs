---
needs: [english-clause-output-retrofit]
---
**Retrofit the ability backend onto truthful products, sums, and sequences.**

Bind A01 `Ability` directly as its header plus `AbilityKind` sum instead of
eleven mutually exclusive optional payloads. Represent costs and keyword lines
as checked nonempty sequences whose types carry separator topology. Add general
typed sum/variant and nonempty-sequence compiler support where the declarations
need it; do not add A01-only reflection or ordinal tables.

Keep the separate multiline ability backend, owner-gated ingress, exact surface
witnesses, recovery, guard-rank ambiguity, and nested construction provenance.
Delete manual legacy serializers and association tests, option-tuple adapters,
first/rest vector shuffling, dummy linearization used only to rediscover form
ordinals, string/`Any` renderer dispatch, and duplicated decision synthesis.
Migrate the typed construction projection, spelling lexicon, inspect, and all
active consumers in the same change.

Standard constraints apply.
