---
needs: [engine-resolve-actions]
---
Countering a triggered or activated ability removes it from the stack and puts
it nowhere — an ability isn't a card, so it goes to no zone ([CR#701.6a]). Needs
a cessation path distinct from normal resolution (a countered ability never
resolves). Completes ward's `Counter` verb, which can target a spell *or* an
ability.
