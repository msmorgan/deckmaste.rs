---
needs: [engine-history-tallies]
design: true
---
A `Count` form for filtered historical counts over the history log (e.g. "draw a
card for each creature that died this turn"). The `engine-history-tallies` log
already retains the data; this adds the grammar variant + eval arm. Wants its own
design dialogue (object identity over the window).
