---
needs: [english-v2-reminder-normalization]
---
Modal and labelled forms. Scope: modal blocks ("Choose one —" and variants,
bulleted modes, "Choose one or more", pawprint/paw-cost modal markers),
labelled ability forms (chapter/level/class labels as document structure),
repetition markers, and quoted granted-ability text ("gains '{T}: ...'")
as a document element whose quoted interior parses with the same grammar.

Acceptance: gates green with the ratchet strictly up; zero ties and
exceptions; byte-exact both directions with total ownership; no process
artifacts in tracked source; genuine tie or contradiction = STOP-and-report.
Standard constraints apply.

Landed (2026-09-02): one label mechanism (`BlockLabel`: ability words and
Saga chapter numerals), one modal block (head sum of clause + em-dash,
sentence sequence, or keyword line; modes with bullet or one weighted
marker covering Spree `+ {2} —` and pawprint `{P}{P} —`), quoted grants as
nested document blocks behind a nominal `with` postmodifier, leveler bands
and Class level bars. Heads and the repetition sentence parse as language
(no chooser/bounds vocab, no sentence literal). 14,259 → 14,789 covered,
0 lost. Review verdict STANDS — CONDITIONALLY; the conditions are routed
to `english-v2-quoted-block-sentence-terminator` (sentence-final quoted
blocks, 312 faces) and the 14-10 fold-ins.
