---
needs: []
---
**Reconcile the landed shared-deontic coordination family.** The executable
Family E work from this residue cluster had already landed in change
`rpwtulnt` without closing its owning follow-up ticket. Preserve its measured
result and move the remaining design-heavy families to
`english-coordination-structural-design`.

## Completion

- Confirmed that a subject-shared positive predicate followed by a negated
  modal predicate lowers as one n-ary `PredicateExpression::Coordinated`, with
  the latter member represented as `Predicate::Deontic`. Both active and
  passive members, a modal first member, VP ellipsis, `by` adjuncts, and typed
  `except by` tails have source-independent structural tests.
- The landed round recovered 122 clause spans across 117 faces. A fresh
  2026-07-29 unknown-phrase audit finds no unresolved row containing
  `can't be blocked except by`.
- Only three unresolved rows still contain the narrower text
  `and can't be blocked`: Ratonhnhaketon and Akawalli are larger fronted
  `as long as` constructions, while Rope is a three-member Oxford predicate
  list. The ordinary two-member witnesses, including Dust Corona and Agility
  Bobblehead, inspect as typed coordinated predicates rather than recovery.
- Moved heterogeneous quoted-ability lists, per-member conditioned Oxford
  lists, and the Sycorax appositive satellite to the design-tagged successor;
  they are not regressions in the landed modal-member machinery.
