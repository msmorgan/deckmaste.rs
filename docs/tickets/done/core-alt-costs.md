---
needs: []
design: true
---
`AlternativeCost` on `May(Cast(cost: …))` and `CostChange::Additional{optional}`
landed as shapes. Remaining: keyword macros over them (flashback, evoke,
overload, madness, kicker) and "if the X cost was paid" linkage.
