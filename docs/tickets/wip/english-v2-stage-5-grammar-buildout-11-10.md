---
needs: [english-v2-stage-5-grammar-buildout-10-10]
---
Keyword lines and ability words. Scope: the keyword-line document block
(bare "Flying", comma series "Flying, haste", parametrized forms "Equip {2}",
"Ward {2}", protection-from phrases), keyword-ability
vocabulary consumed from the graduated stub declarations (grammar
contributions, not per-keyword constructions), ability-word lead-ins
("Landfall — ..."), and the deferred attachment determinative rows (three
exist today; audit the attested set). NOTE (landing-review corrections):
the stub inventory explicitly excludes ability words — minting the
ability-word declaration surface is in scope here, not assumed. All 195
graduated stubs are grammar: FixedKeyword with no parameter lists, so the
parametrized keyword-line forms ("Equip {2}", "Ward {2}") require the
macro-RON parameter schema plus a stub authoring pass — in scope, name it
in the plan.

Fold-in (landing-review finding 1): CounterKind and Designation
construction consumers land here — today they are typed loaders with no
construction slot ("shield counter" and "the monarch" get zero claims).
Reconcile hand vocabulary against declarations without coverage loss
(4 vocab members lack declarations, 21 declarations lack vocab entries)
and delete the superseded hand vocabularies once consumed.

Opening small fixes carried from contract-landing-review.md: Zero
licenses mass nominals (delete the two mass bypass constructions);
declare These (ProximalDemonstrative plural — attestation is provenance,
never a corpus filter); dissolve the TwoPlus-guarded lexicalised
quantities into general quantity determinatives; delete stale VerbLexeme
strings in xtask report.rs; drop dead _name/_valence params in the core
verb seed.

This is the
french-vanilla unlock: most currently-failing creature faces gate on this
block and nothing else.

Every keyword line resolves through the declaration environment; a keyword
with no declared grammar contribution stays unsupported and silent — no
hardcoded keyword surfaces. Determiner contributions from attach macros ride
the existing sole-determiner class.

Acceptance: gates green with the ratchet strictly up; zero ties and
exceptions; byte-exact both directions with total ownership; no process
artifacts in tracked source; genuine tie or contradiction = STOP-and-report.
Standard constraints apply.
