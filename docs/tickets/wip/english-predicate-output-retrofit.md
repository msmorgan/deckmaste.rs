---
needs: [english-phrase-output-retrofit]
---
**Retrofit predicate and nonfinite outputs without weakening lexical laws.**

Keep V01's typed valency, voice, objects, complements, adjuncts, particles,
mana forms, and checked construction as the canonical predicate core. Seal raw
mutation that bypasses those laws, normalize redundant wrapper/projection
shapes, and expose generated construction roles rather than serialized layout.

Keep F01 infinitives. Replace the gerund clause's generic dependent-attachment
vector with the actual recursive, checked `rather than` relation it admits, so
impossible attachment positions and punctuation cannot inhabit the output.
Migrate predicate/nonfinite consumers, spelling frames, rendering, recovery,
and tests atomically; delete legacy-layout and unused facade assertions while
retaining valency, agreement, ordering, ambiguity, inversion, and rejection
coverage.

Standard constraints apply.
