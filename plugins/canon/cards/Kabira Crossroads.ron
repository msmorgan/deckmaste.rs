Normal(
  name: "Kabira Crossroads",
  types: [
    Land,
  ],
  abilities: [
    Static(Replacement(AsEnters(Tap(This)))),
    Triggered(event: ThisEnters, effect: GainLife(2)),
    Activated(cost: [Tap], effect: AddMana(1, White)),
  ],
)
