// {0} artifact: "{1}: draw a card. Activate only as a sorcery."
// the SorcerySpeed condition macro gates the activation window.
Normal(
    name: "Artifact sorcery-speed DrawCards",
    mana_cost: [],
    types: [Artifact],
    abilities: [
        Activated(cost: [Mana([Generic(1)])], condition: SorcerySpeed, effect: Draw(1)),
    ],
)
