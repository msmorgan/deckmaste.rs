// `Owner` takes a predicate, so the reference parameter must be wrapped in
// `Ref`.
(
    name: "InHand",
    kinds: [Predicate],
    params: [Reference],
    body: And([InZone(Hand), Owner(Ref(Param(0)))]),
)
