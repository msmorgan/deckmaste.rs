(
    name: "Player",
    template: "player",
    kinds: [Predicate],
    // `DealsDamageToEach`'s second param is this macro's `Predicate` type
    // (`body: Each(binder: Existing(SelectAll(Param(1))), ...)`); "each
    // player" (Flame Rift, [CR#608.2d]) recovers through this bare-noun
    // frame — no guard, no hole.
    frames: ["player"],
    body: Kind(Player),
)
