(
    name: "RevealUntil",
    kinds: [OneShotEffect],
    params: { "whose": Any, "matches": Any, "body": Any },
    body: RevealUntil(whose: Param(whose), matches: Param(matches), body: Param(body)),
)
