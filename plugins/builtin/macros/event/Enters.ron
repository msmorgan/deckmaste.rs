// "Enters" = a permanent enters the battlefield ([CR#603.6]).
// Usage: Enters(what) — e.g. Enters(Type(Creature)), Enters(Ref(This)).
(
    name: "Enters",
    template: "${0} enters",
    kinds: [Event],
    params: [Filter],
    body: ZoneMove(what: Param(0), to: Battlefield),
)
