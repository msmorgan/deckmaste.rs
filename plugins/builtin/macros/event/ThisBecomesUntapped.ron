// "ThisBecomesUntapped" = this object transitions tapped -> untapped
// ([CR#603.2e]). Mirrors event/ThisBecomesTapped.
(
    name: "ThisBecomesUntapped",
    kinds: [EventFilter],
    body: StateBecame(of: Ref(This), becomes: Untapped),
)
