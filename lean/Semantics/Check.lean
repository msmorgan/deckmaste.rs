import Semantics.Check.Card

/-!
# Semantics.Check

The checker: `Card.check : Card → List Refusal`, the layers beneath it, and `Spelled`. This is
what the Idris put in constructor types; see the README.
-/
