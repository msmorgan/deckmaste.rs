import Experimental.Proofs.Description
import Experimental.Proofs.Refresh

/-!
# Experimental.Proofs

The pin suites: the Idris `Proofs<Family>` modules as `decide` theorems over the checker, one
module per grammar family. `lake build ExperimentalProofs` runs them all.
-/
