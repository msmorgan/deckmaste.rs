#!/bin/sh
# Full gate: syntax, checker, bench, and every pin suite; no errors and no warnings.
# Inner loop: `lake build Experimental` (skips the pin suites).
cd "$(dirname "$0")/.." || exit 1
exec lake build --wfail "$@"
