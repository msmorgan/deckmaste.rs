// "Flying, deathtouch. Whenever Damocles Base deals combat damage to a
// player, that player faces a villainous choice — They sacrifice a
// nontoken creature of their choice, or they lose 2 life and you draw two
// cards. Crew 3" [CR#701.55a]. The two "that player" references after the
// trigger are modeled as a fresh target, matching this corpus's existing
// `nivMizzetGuildpactTrigger` precedent for a trigger's own participant.
SingleFaced(face: (
    characteristics: (
        name: "Damocles Base, Sword of Kang",
        supertypes: [Legendary],
        cost: [
            4,
            Black,
        ],
        types: [Artifact],
        subtypes: [vehicle],
        power: 5,
        toughness: 5,
        text: [
            keyword("Flying"),
            keyword("Deathtouch"),
            triggered(event: damage( kind: CombatOnly, source: asType(type: Creature, subject: this, subtype: None), patient: described(determiner: A(Unmarked), predicate: anyPlayer), ), instruction: faceAVillainousChoice( sacrifice( described( determiner: A(Unmarked), predicate: and([ hasType(Creature), not(isToken), hasPossessor( axis: Controller, possessor: described(determiner: Target(range(low: 1, high: 1)), predicate: anyPlayer), ), ]), ), described(determiner: Target(range(low: 1, high: 1)), predicate: anyPlayer), ), sequentially([ changeLife( delta: down(2), agent: described(determiner: Target(range(low: 1, high: 1)), predicate: anyPlayer), ), draw(amount: 2, agent: you), ]), described(determiner: Target(range(low: 1, high: 1)), predicate: anyPlayer), )),
            keywordNumber(label: "Crew", amount: 3),
        ],
    ),
))
