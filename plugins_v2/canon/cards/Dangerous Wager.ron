// "Discard your hand, then draw two cards." Your hand is every card in it, so
// the discarded phrase is the whole-hand description [CR#701.9a].
SingleFaced(face: (
    characteristics: (
        name: "Dangerous Wager",
        cost: [
            1,
            Red,
        ],
        types: [Instant],
        text: [
            spell(
                timing: None,
                instruction: sequentially(steps: [
                    discard(
                        described(
                            determiner: All,
                            predicate: and(conjuncts: [
                                isCard,
                                inZone(zone: zone(zone: Hand, scope: PossessedBy(you))),
                            ]),
                        ),
                        you,
                    ),
                    draw(amount: 2, agent: you),
                ]),
            ),
        ],
    ),
))
