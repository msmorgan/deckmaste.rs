// A reach counter ([CR#122.1b]): its bearer gains reach at layer 6
// ([CR#613.1f]) — the FlyingCounter pattern with a different keyword.
CounterKind(
    name: "reachCounter",
    params: [],
    spelling: "reach",
    grammar: FixedTerm(surface: "reach"),
    body: Counter(
        kind: Keyword(keyword: "Reach"),
        holder: Object,
        confers: [
            Property(spec: AbilityGrant(
                subject: This,
                ability: Keyword(keyword: "Reach", params: [], body: []),
            )),
        ],
    ),
)
