// A first strike counter ([CR#122.1b]): its bearer gains first strike at layer 6
// ([CR#613.1f]) — the FlyingCounter pattern with a different keyword.
CounterKind(
    name: "firstStrikeCounter",
    params: [],
    spelling: "first strike",
    grammar: FixedTerm(surface: "first strike"),
    body: Counter(
        kind: Keyword(keyword: "FirstStrike"),
        holder: Object,
        confers: [
            Property(spec: AbilityGrant(
                subject: This,
                ability: Keyword(keyword: "FirstStrike", params: [], body: []),
            )),
        ],
    ),
)
