// A menace counter ([CR#122.1b]): its bearer gains menace at layer 6
// ([CR#613.1f]) — the FlyingCounter pattern with a different keyword.
CounterKind(
    name: "menaceCounter",
    params: [],
    spelling: "menace",
    grammar: FixedTerm(surface: "menace"),
    body: Counter(
        kind: Keyword(keyword: "Menace"),
        holder: Object,
        confers: [
            Property(spec: AbilityGrant(
                subject: This,
                ability: Keyword(keyword: "Menace", params: [], body: []),
            )),
        ],
    ),
)
