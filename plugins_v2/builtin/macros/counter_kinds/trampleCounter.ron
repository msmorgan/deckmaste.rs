// A trample counter ([CR#122.1b]): its bearer gains trample at layer 6
// ([CR#613.1f]) — the FlyingCounter pattern with a different keyword.
CounterKind(
    name: "trampleCounter",
    params: [],
    spelling: "trample",
    grammar: FixedTerm(surface: "trample"),
    body: Counter(
        kind: Keyword(keyword: "Trample"),
        holder: Object,
        confers: [
            Property(spec: AbilityGrant(
                subject: This,
                ability: Keyword(keyword: "Trample", params: [], body: []),
            )),
        ],
    ),
)
