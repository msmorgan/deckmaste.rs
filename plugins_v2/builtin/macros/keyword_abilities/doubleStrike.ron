KeywordAbility(
    name: "doubleStrike",
    params: [],
    spelling: "double strike",
    grammar: FixedKeyword(
        surface: "double strike",
    ),
    // Settled empty. "Double strike is a static ability that modifies the rules for the
    // combat damage step" [CR#702.4a]; the entry states the two-step damage rule
    // [CR#702.4b] rather than quoting an expansion.
    // The entry defines only how the game treats the keyword (ruling 2026-09-07).
    body: Keyword(keyword: "DoubleStrike", params: [], body: []),
)
