KeywordAbility(
    name: "harmonize",
    params: [Cost],
    spelling: "harmonize",
    grammar: FixedKeyword(
        surface: "harmonize",
    ),
    // STOP. Harmonize's second static reduces the total cost "by an amount of generic
    // mana equal to the tapped creature's power" [CR#702.180a]. Nothing links it back
    // to the creature the first static's cost tapped — the same missing linked
    // reference that stops emerge.
    body: Keyword(keyword: "Harmonize", params: [Cost(Param(0))], body: []),
)
