KeywordAbility(
    name: "Compleated",
    params: [],
    spelling: "compleated",
    grammar: FixedKeyword(
        surface: "compleated",
    ),
    // STOP. Compleated reads the life paid for Phyrexian mana symbols in the cost
    // actually paid [CR#702.150a]. No `Amount` projects that, so the loyalty
    // replacement's "minus two for each of those mana symbols" cannot be written.
    body: Keyword(keyword: "Compleated", params: [], body: []),
)
