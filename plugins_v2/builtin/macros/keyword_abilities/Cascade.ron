KeywordAbility(
    name: "Cascade",
    params: [],
    spelling: "cascade",
    grammar: FixedKeyword(
        surface: "cascade",
    ),
    // STOP. Cascade's expansion says "You may cast that card without paying its
    // mana cost if the resulting spell's mana value is less than this spell's mana
    // value" [CR#702.85a]. No `NounPhrase` denotes the spell a permitted cast would
    // produce, so its mana value cannot be compared.
    body: Keyword(keyword: "Cascade", params: [], body: []),
)
