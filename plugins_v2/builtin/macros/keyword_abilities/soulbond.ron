KeywordAbility(
    name: "soulbond",
    params: [],
    spelling: "soulbond",
    grammar: FixedKeyword(
        surface: "soulbond",
    ),
    // STOP. Soulbond's two triggered abilities both PAIR two creatures "for as long as
    // both remain creatures on the battlefield under your control" [CR#702.95a], and
    // abilities elsewhere read whether a creature is paired and which creature it is
    // paired with [CR#702.95b]. Nothing expresses that link: `designations/` has no
    // paired designation, `Predicate::Attachment` reads Auras and Equipment, and no
    // instruction pairs two objects.
    body: Keyword(keyword: "Soulbond", params: [], body: []),
)
