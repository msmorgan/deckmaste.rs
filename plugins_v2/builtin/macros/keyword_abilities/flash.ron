KeywordAbility(
    name: "flash",
    params: [],
    spelling: "flash",
    grammar: FixedKeyword(
        surface: "flash",
    ),
    // STOP. "Flash" means "You may play this card any time you could cast an
    // instant." [CR#702.8a] `PlayTiming` has no value for that window; the grammar
    // reaches it only as `AsThough::Of` over `hasKeyword "Flash"`, which cannot
    // define flash itself.
    body: Keyword(keyword: "Flash", params: [], body: []),
)
