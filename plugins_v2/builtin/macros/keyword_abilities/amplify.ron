KeywordAbility(
    name: "amplify",
    params: [Amount],
    spelling: "amplify",
    grammar: FixedKeyword(
        surface: "amplify",
    ),
    // STOP. "Amplify N" means "As this object enters, reveal any number of cards
    // from your hand that share a creature type with it. …" [CR#702.38a] No
    // `Predicate` reads "shares a creature type with <subject>": the sharing
    // comparison over a subtype axis has no constructor.
    body: Keyword(keyword: "Amplify", params: [Number(Param(0))], body: []),
)
