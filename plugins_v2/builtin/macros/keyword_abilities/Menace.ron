KeywordAbility(
    name: "Menace",
    params: [],
    spelling: "menace",
    grammar: FixedKeyword(
        surface: "menace",
    ),
    // "A creature with menace can't be blocked except by two or more creatures."
    // [CR#702.111b] The exception is a cardinality bound, not a description of the
    // blockers.
    body: Keyword(
        keyword: "Menace",
        params: [],
        body: [
            Static(
                spec: DeonticRule(
                    subject: AsType(type: Creature, subject: This, subtype: None),
                    compulsion: Forbid,
                    deeds: [Core(deed: Block)],
                    role: Patient,
                    bound: MoreThan(amount: Lit(value: 1)),
                    patient: Counterpart(
                        patient: Described(determiner: All, predicate: HasType(type: Creature)),
                    ),
                    as_though: None,
                    rider: NoRider,
                ),
            ),
        ],
    ),
)
