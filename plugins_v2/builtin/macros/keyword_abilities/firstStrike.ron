KeywordAbility(
    name: "firstStrike",
    params: [],
    spelling: "first strike",
    grammar: FixedKeyword(
        surface: "first strike",
    ),
    // Settled empty. "First strike is a static ability that modifies the rules for the combat
    // damage step" [CR#702.7a]; the entry states the two-step damage rule
    // [CR#702.7b] rather than quoting an expansion.
    // The entry defines only how the game treats the keyword (ruling 2026-09-07).
    body: Keyword(keyword: "FirstStrike", params: [], body: []),
)
