// "if it isn't <p>", read of the ability just named.
(
    name: "ItIsntAnAbility",
    kinds: [Condition],
    params: {"p": Predicate},
    body: Not(condition: Matches(That(w: Ability), Param(p))),
)
