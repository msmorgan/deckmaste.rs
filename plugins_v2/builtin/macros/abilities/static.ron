// `Ability.Static` under Lean's own name for it: the alias
// `declare_semantic_primitives` generates, so a card writes a macro where the
// basis has a constructor (ADR 12.1).
(
    name: "static",
    kinds: [Ability],
    params: {"spec": StaticSpec},
    body: Static(Param(spec)),
)
