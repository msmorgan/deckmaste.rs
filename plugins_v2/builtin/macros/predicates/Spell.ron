// "spell": an object on the stack that is not an ability [CR#112.1,113.1].
(
    name: "Spell",
    kinds: [Predicate],
    params: [],
    body: And(conjuncts: [InZone(zone: Stack), Not(predicate: AbilityHead(class: AnyOnStack))]),
)
