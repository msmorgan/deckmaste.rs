// Several static clauses sharing one subject, as one instruction.
(
    name: "EstablishFor",
    kinds: [Instruction],
    params: {"subject": NounPhrase, "parts": StaticSpecs, "duration": Duration},
    body: Establish(Conjunction(Param(subject), Param(parts)), Param(duration)),
)
