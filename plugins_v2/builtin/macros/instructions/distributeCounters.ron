// "distribute N <kind> counters among <among>"
(
    name: "distributeCounters",
    kinds: [Instruction],
    params: {"amount": Amount, "kind": CounterKind, "among": NounPhrase},
    body: Distribute(Counters(Param(kind)), Param(amount), Param(among)),
)
