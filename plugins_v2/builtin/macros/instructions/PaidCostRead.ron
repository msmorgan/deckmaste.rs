// "<subject>'s <keyword> cost was paid", read back.
(
    name: "PaidCostRead",
    kinds: [Amount],
    params: {"which": PaidCostName, "window": Lookback, "subject": NounPhrase},
    body: Paid(Readback(Param(which), Param(window)), Param(subject)),
)
