// "<agent> rolls <count> d<sides>" [CR#706.1].
(
    name: "rollDice",
    kinds: [Instruction],
    params: {"count": Nat, "sides": Nat, "agent": Default(NounPhrase, You)},
    body: RollDice(Lit(Param(count)), Sides(Param(sides)), Param(agent)),
)
