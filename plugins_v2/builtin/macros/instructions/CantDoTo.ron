// "<who> can't <deed> <what>"
(
    name: "CantDoTo",
    kinds: [StaticSpec],
    params: {"deed": Deed, "who": NounPhrase, "what": NounPhrase},
    body: DeonticRule(Param(who), Forbid, [Param(deed)], Agent, None, Counterpart(patient: Param(what)), None, NoRider),
)
