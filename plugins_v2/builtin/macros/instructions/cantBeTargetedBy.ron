// "<what> can't be the target of <by>"
(
    name: "cantBeTargetedBy",
    kinds: [StaticSpec],
    params: {"what": NounPhrase, "by_": NounPhrase},
    body: DeonticRule(Param(what), Forbid, [Core(Target)], Patient, None, TargetedBy(Param(by_)), None, NoRider),
)
