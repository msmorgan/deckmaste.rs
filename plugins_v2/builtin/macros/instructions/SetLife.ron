// "<player>'s life total becomes <amount>"
(
    name: "SetLife",
    kinds: [Instruction],
    params: {"amount": Amount, "agent": Default(NounPhrase, You)},
    body: ChangeLife(delta: Set(amount: Param(amount)), agent: Param(agent)),
)
