// "<who> can't <deed> <what>"
(
    name: "cantDoTo",
    kinds: [StaticSpec],
    params: {"deed": Deed, "who": NounPhrase, "what": NounPhrase},
    body: DeonticRule(Param(who), Forbid, [Param(deed)], Agent, None, Counterpart(Param(what)), None, NoRider),
)
