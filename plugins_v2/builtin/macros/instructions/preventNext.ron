// "Prevent the next N <kind> damage that would be dealt <scope> [duration]."
(
    name: "preventNext",
    kinds: [Instruction],
    params: {"kind": DamageKind, "scope": DamageScope, "amount": Amount, "duration": Duration},
    body: Establish(DamageRule(Param(kind), Unattributed, Param(scope), Prevent(Shield(Param(amount)), None), Repeatedly), Param(duration)),
)
