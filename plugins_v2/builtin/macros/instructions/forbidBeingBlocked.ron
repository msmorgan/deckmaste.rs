// "<subject> can't be blocked [this turn]"
(
    name: "forbidBeingBlocked",
    kinds: [Instruction],
    params: {"subject": NounPhrase, "duration": Duration},
    body: Establish(DeonticRule(Param(subject), Forbid, [Core(Block)], Patient, None, NoPatient, None, NoRider), Param(duration)),
)
