KeywordAbility(
    name: "Reach",
    params: [],
    spelling: "reach",
    grammar: FixedKeyword(
        surface: "reach",
    ),
    // Settled empty. Reach's content is stated inside flying's blocking restriction
    // [CR#702.17b,702.9b] — "a creature with flying can't be blocked except by
    // creatures with flying and/or reach". The entry quotes no expansion of reach
    // itself.
    // The entry defines only how the game treats the keyword (ruling 2026-09-07).
    body: Keyword(keyword: "Reach", params: [], body: []),
)
