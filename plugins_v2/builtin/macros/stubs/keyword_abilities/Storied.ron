KeywordAbility(
    name: "Storied",
    params: [],
    spelling: "storied",
    grammar: FixedKeyword(
        surface: "storied",
    ),
    // STOP. "Storied" means "ANY TIME you control three or more permanents that are
    // artifacts, Sagas, and/or legendary and you don't have an enduring story, you
    // have an enduring story for the rest of the game." [CR#702.195a] No `StaticSpec`
    // performs an instruction when a state holds; `DeonticRider::StateBased` is the
    // only state-checked clause and its `StateBasedCause` has the single value
    // `NonpositiveLife` (the cause Ascend carries — city's blessing and enduring
    // story are the same shape).
    body: Keyword(keyword: "Storied", params: [], body: []),
)
