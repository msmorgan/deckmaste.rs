// "the last <kind> counter is removed from <subject>"
(
    name: "LastCounterRemoved",
    kinds: [GameEvent],
    params: {"kind": CounterKind, "subject": NounPhrase},
    body: CounterEvent(Removed, Param(kind), Param(subject), Emptying, None, false),
)
