// "the last <kind> counter is removed from <subject> by <who>"
(
    name: "lastCounterRemovedBy",
    kinds: [GameEvent],
    params: {"kind": CounterKind, "subject": NounPhrase, "who": NounPhrase},
    body: CounterEvent(Removed, Param(kind), Param(subject), Emptying, Param(who), false),
)
