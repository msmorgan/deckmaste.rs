[package]
name = "deckmaste_core"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
macro_ron = { version = "0.1.0", path = "../macro_ron", features = ["derive"] }
ron = "0.12.1"
serde = { version = "1.0.228", features = ["derive"] }
