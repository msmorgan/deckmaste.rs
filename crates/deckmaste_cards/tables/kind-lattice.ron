// GENERATED by idris/src/EmitTables.idr — do not edit by hand.
// Regenerate with idris/scripts/emit-tables; CI diffs the output.
// The RefKind join-semilattice (`\/`): OneOf widens object|player to Any ([CR#115.4]).
(
    joins: [
        (left: Empty, right: Empty, join: Empty),
        (left: Empty, right: Object, join: Object),
        (left: Empty, right: Player, join: Player),
        (left: Empty, right: Any, join: Any),
        (left: Object, right: Empty, join: Object),
        (left: Object, right: Object, join: Object),
        (left: Object, right: Player, join: Any),
        (left: Object, right: Any, join: Any),
        (left: Player, right: Empty, join: Player),
        (left: Player, right: Object, join: Any),
        (left: Player, right: Player, join: Player),
        (left: Player, right: Any, join: Any),
        (left: Any, right: Empty, join: Any),
        (left: Any, right: Object, join: Any),
        (left: Any, right: Player, join: Any),
        (left: Any, right: Any, join: Any),
    ],
)
