// Sorted singular `That(Card)` with NO compatible antecedent in scope —
// empty stack, no binder frame [CR#608.2d].
Normal(
  name: "Reject That Unbound",
  types: [Sorcery],
  abilities: [
    Spell(effect: Destroy(That(Card))),
  ],
)
