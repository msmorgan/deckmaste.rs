// Twin of Spec.idr `tBadThatOutsideWith`: the plural anaphor `They` with no
// compatible Many antecedent in scope [CR#608.2d].
Normal(
  name: "Reject Group That Unbound",
  types: [Sorcery],
  abilities: [
    Spell(effect: Each(binder: Existing(They), effect: Destroy(It))),
  ],
)
