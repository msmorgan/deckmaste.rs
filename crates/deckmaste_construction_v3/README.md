# V3 construction declarations

`constructions!` is the thin proc-macro shell over
[`deckmaste_construction_v3_core`](../deckmaste_construction_v3_core/README.md).
That README documents declaration syntax, generated interfaces and compiler
obligations. The compiled consumer in `tests/generated.rs` exercises every
projection through this public macro.
